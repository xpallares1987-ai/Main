name: Multi-Project Deployment

on:
  push:
    branches: [ main ]

permissions:
  contents: read
  pages: write
  id-token: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build-all:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Install dependencies (Monorepo)
        run: npm install --legacy-peer-deps

      - name: Prepare Out Dir
        run: mkdir -p out

      # BUILD BPMN Modeler
      - name: Build BPMN Modeler
        run: |
          cd BPMN-Modeler
          npm run build -- --base ./
          mkdir -p ../out/BPMN-Modeler
          if [ -d "dist" ]; then cp -r dist/* ../out/BPMN-Modeler/; fi

      # BUILD Shipment Tracker
      - name: Build Shipment Tracker
        run: |
          cd Shipment-Tracker
          npm run build -- --base ./
          mkdir -p ../out/Shipment-Tracker
          if [ -d "dist" ]; then cp -r dist/* ../out/Shipment-Tracker/; fi

      # BUILD Logistics Dashboard
      - name: Build Logistics Dashboard
        run: |
          cd Logistics-Dashboard
          npm run build -- --base ./
          mkdir -p ../out/Logistics-Dashboard
          if [ -d "dist" ]; then cp -r dist/* ../out/Logistics-Dashboard/; fi

      # COPY Root Index
      - name: Copy Root Index
        run: cp index.html out/

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './out'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
