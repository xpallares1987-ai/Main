{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "globalPassThroughEnv": ["NODE_ENV", "GITHUB_TOKEN"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [
        "dist/**",
        ".next/**",
        "!.next/cache/**",
        "out/**"
      ],
      "inputs": [
        "src/**",
        "public/**",
        "next.config.mjs",
        "vite.config.ts",
        "tsconfig.json",
        "package.json"
      ]
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**"],
      "inputs": ["src/**", "test/**", "__tests__/**", "vitest.config.ts"]
    },
    "lint": {
      "outputs": [],
      "inputs": ["src/**", ".eslintrc*", "eslint.config.js"]
    },
    "type-check": {
      "outputs": [],
      "inputs": ["src/**", "tsconfig.json"]
    },
    "format": {
      "outputs": [],
      "inputs": ["src/**", ".prettierrc*", "prettier.config.js"]
    }
  }
}
